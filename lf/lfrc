# set previewer ~/.config/lf/pv.sh
# map i $~/.config/lf/pv.sh $f | less -R

# set previewer lf-previewer
# set cleaner lf-cleaner

set icons true

map i set hidden!

map o &ripdrag -A -x $fx
map O &ripdrag